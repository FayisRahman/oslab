
int main(){
decl
    int temp;
    str name;
enddecl
begin

    temp = exposcall("Fork");
    
    if(temp != -1) then
        if(temp == 0 ) then
            temp = exposcall("Exec","even.xsm");
        else
            temp = exposcall("Exec" , "odd.xsm");
        endif;
    endif;

    return 0;
end
}